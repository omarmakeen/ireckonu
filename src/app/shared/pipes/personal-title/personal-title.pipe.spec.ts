import { PersonalTitlePipe } from './personal-title.pipe';

describe('PersonalTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new PersonalTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
