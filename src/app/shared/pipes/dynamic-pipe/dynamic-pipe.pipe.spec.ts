import { DynamicPipePipe } from './dynamic-pipe.pipe';

describe('DynamicPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicPipePipe();
    expect(pipe).toBeTruthy();
  });
});
